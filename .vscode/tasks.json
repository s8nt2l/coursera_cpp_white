{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++ -g ${relativeFile} -o ${fileBasenameNoExtension}.out",
            "args": [],
            "problemMatcher": []
        }
    ]
}
